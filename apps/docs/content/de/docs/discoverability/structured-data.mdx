---
title: Strukturierte Daten
description: >-
  Das schema.org-JSON-LD, das jede Seite mitbringt – WebSite-, Artikel- und Breadcrumb-Knoten – plus die optionalen Organization- und SoftwareApplication-Blöcke, die Suchmaschinen und Agents verraten, wer du bist.
---

Blume gibt in jedem `<head>` einer Seite [schema.org](https://schema.org)-JSON-LD aus, damit Suchmaschinen deine Inhalte verstehen. Standardmäßig aktiviert:

```ts blume.config.ts lineNumbers
seo: {
  structuredData: true,
}
```

Jede Seite enthält:

- einen **WebSite**-Knoten für die Identität der Website,
- die Seite als **Artikel** – `BlogPosting` für Blogbeiträge, `TechArticle` für Changelog und Dokumentation – mit Beschreibung und Veröffentlichungsdatum,
- eine **BreadcrumbList**, die aus dem Navigationspfad aufgebaut wird.

URLs sind absolut, wenn [`deployment.site`](/docs/deployment) gesetzt ist. Seiten, die mit [`seo.noindex`](/docs/discoverability/metadata#per-page-overrides) markiert sind, werden übersprungen. Wenn [`lastModified`](/docs/configuration#last-modified) aktiv ist, wird das Datum jeder Seite zusätzlich als `dateModified` ausgegeben.

## Identität der Website [#site-identity]

Der WebSite-Knoten sagt, dass eine Website existiert; er sagt nicht, _was_ sie ist oder _wer_ sie betreibt – und genau dafür lesen KI-Agents JSON-LD, bevor sie dich empfehlen oder zitieren. Zwei optionale Blöcke füllen das auf, beide brauchen `deployment.site` (die Knoten tragen absolute Identifier):

```ts blume.config.ts lineNumbers
seo: {
  organization: {
    name: "Acme", // defaults to the site title
    email: "hello@acme.com",
    telephone: "+1 555 0100",
    address: { addressLocality: "Sydney", addressCountry: "AU" },
    logo: "/logo.svg",
    sameAs: ["https://github.com/acme", "https://x.com/acme"],
  },
  software: {
    license: "MIT",
    operatingSystem: "Node.js 22+",
    price: 0, // emitted as an Offer; 0 marks it free
    sameAs: ["https://www.npmjs.com/package/acme"],
  },
}
```

`organization` fügt jeder Seite einen **Organization**-Knoten hinzu – die WebSite- und Artikel-Knoten verweisen als `publisher` darauf – mit E-Mail und Telefonnummer als `ContactPoint` (`contactType` ist standardmäßig `"customer support"`) und der Adresse als `PostalAddress`, den beiden Feldern, nach denen Prüfungen zur Unternehmensverifizierung suchen. `name` und `url` übernehmen standardmäßig die Werte der Website; ein root-relatives `logo` wird wie jede Seiten-URL absolut gemacht.

`software` fügt der Startseite einen **SoftwareApplication**-Knoten hinzu: Name und Beschreibung des Produkts (standardmäßig die der Website), `applicationCategory` (Standard `"DeveloperApplication"`), Betriebssystem, Lizenz, ein `Offer`, wenn `price` gesetzt ist, sowie die Registry- oder Repository-URLs in `sameAs`. Übergib `software: true`, um alle Standardwerte zu übernehmen. Die Organisation wird, sofern konfiguriert, als deren `publisher` angegeben.

## Eigene Seiten [#custom-pages]

Eine eigene [`.astro`-Seite](/docs/advanced/custom-pages), die über `PageLayout` gerendert wird, gibt denselben `WebSite`-Graphen aus, damit eine Marketing-Startseite nicht die eine URL ohne strukturierte Daten ist. Übergib `structuredDataEnabled={config.structuredData}`, um sie mit dieser Einstellung synchron zu halten, oder `structuredDataEnabled={false}`, um sie nur für diese Seite abzuschalten.
